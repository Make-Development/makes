const Tools = (props) => {
    return (
        <>
           <div className="mk-control_tools ml-md-auto active" >
                {props.children}
            </div>
        </>
    );
}

export default Tools;
