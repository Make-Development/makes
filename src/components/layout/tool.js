import React from 'react';

const tool = (props) => {
    return (
        <>
            <div className="   mk-control_tools ml-md-auto" >
                {props.children}
            </div >
        </>
    );
}

export default tool;
